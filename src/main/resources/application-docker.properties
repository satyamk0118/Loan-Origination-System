# This profile is auto-activated when running inside Docker via env vars.
# The docker-compose.yml passes SPRING_DATASOURCE_* env vars which Spring Boot
# automatically maps — no manual edits needed.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
